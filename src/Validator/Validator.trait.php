<?php

namespace RoyalMail\Validator


/**
 * Trait to provice YML schema based validation for requests (and possibly responses).
 * 
 */
trait Validator {

  /**
   * Validate the gives values against the schema given.
   * 
   */
  function validateAgainstSchema($schema, $values) {

  }
}